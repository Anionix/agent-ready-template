# CLAUDE.md

@AGENTS.md

## Claude Code notes / Claude Code向け補足

English:

- Use `AGENTS.md` as the source of truth.
- Use plan mode for large changes.
- Prefer concise summaries.
- Do not use broad permissions unless explicitly requested.
- Do not read or modify secrets.

日本語:

- `AGENTS.md` を正本として扱う。
- 大きな変更ではplan modeを使う。
- 要約は簡潔にする。
- 明示的に依頼されない限り、広い権限を使わない。
- secretを読んだり変更したりしない。
